#!/bin/bash

COMMAND=$1

if [ -z "$COMMAND" ]; then
    ./doravel --help
    exit 1
fi

shift

if [ "$COMMAND" = "test" ]; then
    ./doravel artisan --prod test $@
    exit 1
fi

./doravel $COMMAND --prod $@