#!/bin/sh

echo $$ > /var/run/octaned.pid

cleanup() {
    rm -f /var/run/octaned.pid
    exit 0;
}

trap cleanup SIGTERM
trap cleanup SIGQUIT
trap cleanup EXIT

artisan octane:start --server=swoole $@
