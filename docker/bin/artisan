#!/bin/ash

if [ ! -d "./vendor" ]; then
    composer install
fi

APP_ENV=${APP_ENV:-'local'}
COMMAND="/usr/local/bin/php /var/www/artisan"

if [ "$APP_ENV" == "local" ]; then
    $COMMAND $@
else
    $COMMAND --env="$APP_ENV" $@
fi